# Copy this file to .env and fill in the values.

X_CLIENT_ID=
# Optional for PKCE public clients; set if your app is confidential.
X_CLIENT_SECRET=

# Must match the redirect URI configured in your X app.
X_REDIRECT_URI=http://127.0.0.1:8080/callback

# Space-separated scopes.
X_SCOPES=tweet.read tweet.write users.read media.write offline.access

# Override if needed.
X_API_BASE_URL=https://api.twitter.com
X_AUTH_URL=https://twitter.com/i/oauth2/authorize
X_TOKEN_URL=https://api.twitter.com/2/oauth2/token

# Override data directory if running outside repo root.
XP_DATA_DIR=./data
